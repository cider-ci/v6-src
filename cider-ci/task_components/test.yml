scripts:
  bundle-install:
    exclusive_executor_resource: cider-ci-v6-bundle
    body: |
      #!/usr/bin/env bash
      set -euo pipefail
      # Install only the tools needed for testing (geckodriver is system-wide).
      mise install ruby firefox
      eval "$(mise activate bash)"
      bundle install --quiet

  test:
    start_when:
      bundle-installed:
        script_key: bundle-install
      server-is-ready:
        script_key: server-ready
    body: |
      #!/usr/bin/env bash
      set -euo pipefail
      eval "$(mise activate bash)"
      CI_HTTP_PORT="${CI_SERVER_PORT}" \
      CIDER_CI_DATABASE_NAME="cider_ci_v6_${CIDER_CI_TRIAL_ID}" \
        bundle exec rspec "$CIDER_CI_TASK_FILE"

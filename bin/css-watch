#!/usr/bin/env bash
set -euxo
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
cd $PROJECT_DIR
npx sass --watch --load-path=node_modules --style=compressed --source-map \
    css-src:resources/cider-ci/public/css

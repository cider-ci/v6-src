#!/usr/bin/env bash
set -euxo
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
cd $PROJECT_DIR
rm -rf resources/cider-ci/public/js/
npm ci
npx shadow-cljs release cider-ci
rm -rf resources/cider-ci/public/js/cljs-runtime/

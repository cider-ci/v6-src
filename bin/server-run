#!/bin/bash -eu

set -euo
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
cd $PROJECT_DIR
clj -M -m cider-ci.main --dev-mode true --repl true server run "$@"

# vi: ft=sh

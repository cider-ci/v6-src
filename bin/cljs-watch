#!/usr/bin/env bash
set -euxo
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"
cd $PROJECT_DIR
source $PROJECT_DIR/support/clojure/bin/activate
rm -rf resources/cider-ci/public/js/
npx shadow-cljs watch cider-ci

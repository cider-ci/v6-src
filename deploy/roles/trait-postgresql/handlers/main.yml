---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart trait postgresql
  ansible.builtin.service:
    name: "{{ ci_executor_trait_postgresql_service_name }}"
    state: restarted

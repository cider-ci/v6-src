#!/usr/bin/env bash
set -euo pipefail
PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd ../../.. > /dev/null 2>&1 && pwd -P)"
cd "$PROJECT_DIR"
.venv/bin/ansible-playbook -i deploy/inventories/zhdk deploy/init.yml "$@"

# vi: ft=sh
